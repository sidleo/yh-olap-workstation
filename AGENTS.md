# yh-olap 独立工作站 开发规范

本文件是本项目唯一事实来源。改代码前先读它。

## 项目形态

- **独立 DSH 实例**：`profile/` 是独立 profile 配置源，`start.sh` 安装到 `$DSH_HOME/profiles/yh-olap`（`$DSH_HOME` 默认 `<本项目>/.dsh-home`，真隔离）。`dsh --profile yh-olap --port <port>` 启动。
- **工作站插件**：`plugin/` 是 npm 包 `dsh-yh-olap-workstation`，`src/host.js`（Host）+ `src/client.js`（Client）纯 JS 静态 bundle，无构建。
- **独立性**：本项目是**独立项目**，代码不引用、不依赖任何外部 yh-olap 仓库/installable 目录（删除外部副本后仍可完整运行）。源码内 `// ===== WORKSTATION: xxx =====` 注释是本地功能区块标记，仅供按区块阅读定位，不代表存在外部上游。

## 架构与平台分工

- **Host**（`plugin/src/host.js`）：全部网络/认证/OLAP API 代理、`olap` 模型工具、反向命令队列、workspace 多文件持久化 RPC、sessions 列表 RPC。客户端 RPC 统一 `POST /api/yh-olap/rpc` `{method,args}`。
- **Client**（`plugin/src/client.js`）：`window.__ModuleLoader__.load({id:'dsh-yh-olap-workstation',...})` 导出 `{inject,apply}`；React 经 `require("react")`；RPC 用 fetch；CSS 挂 `<style>` dispose 移除。
- **工作站布局**：CSS `!important` 重排 AppFrame 网格为三列 —— sidebar（会话选择，默认收起 56px 展开条）最左、**rightbar（OLAP，新版 DSH 里旧 details 槽改名为 rightbar）**中、conversation(main) 右；列宽 `--yh-ws-sidebar-w`（轮询 AppFrame inline 首段同步）+ `--yh-ws-cols`（OLAP/会话，2fr 默认、可拖分隔条持久化）；启动 `ctx.layout.openRightbar(true,false)`（旧 `openDetails()` 已删除）并默认 `toggleSidebar()` 收起一次；frame 定位统一走 `wsFrameEl()`（用恒在的 `[data-shell-overlay]` 反推父元素，勿再用状态属性反查）。
- **本地持久化（多文件，per-session）**：`~/.yh-olap/workspace/<sessionId>/{sql,params,notes}/`，每 tab 三份文件（`<tabId>-<name>.sql` / `.json` / `.md`）；SQL 输入 debounce ~800ms 自动保存；**新会话没有历史文件 → 纯新状态（1 个空标签）**；切回旧会话恢复它自己的工作区。
- **会话独立性：面板跟随当前会话**：OSD 的 details slot 切换会话不更新 props.sessionId（实测），OlapPanel 自轮询 sessions 服务 `current`（800ms）切 store；`wsFollowNewSessions`（1s 轮询 ids）检测**新增会话 → 自动 open**（DSH「新会话」按钮只创建不切 current）。

## 关键契约

- 静态适配：package.json `dsh.bundle.patch=./cordis.patch.yml` + `dsh.client.inject=[runtime, ui-slots]`；`cordis.patch.yml` 的 `name` 用 npm 包名 `dsh-yh-olap-workstation`；client `load({id})` 的 id 必须等于 npm 包名。
- **client 插件 id 必须是包名**，勿改。
- **知识库 = `@sidleo3/dsh-wiki`（llm-wiki bundle）**：作为 profile bundle 挂在 `profile/package.template.json`，只在**模型侧**提供 `wiki_*` 工具（list/search/get/create/update/validate/lint/ingest/rules/help/dirs/use）；面板**不浏览知识库**（左栏只剩 库表/收藏），故 host 内不含任何知识库读取 RPC。bundle 目录由 `~/.agents/wiki-registry.json`（命名 bundle + active）解析、默认 `~/.agents/wiki`——隔离 profile 不隔离 `~/.agents`，与主实例共享同一份知识库，无需在 profile patch 里配 `dataDir`。
- 认证/OLAP API 端点、深色主题色板（`--dsw-alias-*`）、多光标/补全等实现契约见本文件「架构与平台分工」与源码内注释（本项目为独立实现，不依赖外部文档目录）。

## 验证流程

1. `node --check plugin/src/host.js`
2. `node -e "new Function(require('fs').readFileSync('plugin/src/client.js','utf8'))"`（client parse）
3. `./start.sh` 启动独立实例，浏览器 http://127.0.0.1:5175 人工验证：
   - 左 sidebar（默认收起 56px 展开条）/ 中 OLAP / 右会话；OLAP 面板自动展开；点 rail 可展开会话列表
   - 工作区 tab：编辑 SQL 自动落盘 `~/.yh-olap/workspace/<sessionId>/sql/*.sql`；重开新会话自动恢复；新会话默认 1 个空标签（无旧内容灌入）
   - 左栏只有 库表/收藏 两 tab（知识库 tab 已移除，知识库只在模型侧用 `wiki_*` 工具查）
   - 会话列表在 sidebar（工作站最左列，默认收起 56px 展开条，点开可看会话/工作区）
   - 聊天里模型可用 `olap` 工具操控面板；知识库可用 `wiki_list`/`wiki_search` 查到 llm-wiki 条目
4. `./stop.sh` 停止
5. **自动落盘功能的自动化验证红线**：编辑器会 debounce 自动保存到 `~/.yh-olap/workspace/<sessionId>/`，**禁止用 headless/CDP 直接驱动真实 5175 的编辑器**（会把用户的标签文件覆盖/删除，已发生过一次）。优先在页面里 append 孤立 DOM 节点做沙箱验证（不触发 store/autosave）；确需驱动真实编辑器时先把 `~/.yh-olap/workspace` 整体移开（`mv workspace workspace.hold`），测完立即还原。

## 更新历史

- 2026-09 **自绘选区两处收尾：`Range` 矩形要按行合并补足行高（否则「锯齿感」）+ 拖选要监听 document 的 `selectionchange`**：用户反馈自绘高亮「背景色边缘有锯齿感」。两个原因：①**`Range.getClientRects()` 是按 span 分段返回的**（4252 字测得 3652 个矩形），且每个矩形高度只有**字形高 15px**（行高 20.15px）→ 逐段画会出现**行内接缝 + 行间条纹**；②小数坐标绝对定位 + 半透明底色 → 边缘被抗锯齿成毛边。修法：按物理行 `row = round((rect.top - ED_PAD_TOP)/ED_LINE_H)` **合并为一行一个矩形**（同行的 left 取 min、right 取 max），上下边直接取整行盒 `ED_PAD_TOP + row*ED_LINE_H`（行盒相邻 → 无缝），并把 left/right/top/bottom **吸附到设备像素**（`Math.round(v*dpr)/dpr`，DPR1 下 95 行矩形整齐相接）。实测 4252 字全选：矩形数 3652 → **95**（= 视觉行数），首个矩形 `top:10px; height:20px`。另一处：**React 的 `onSelect` 是合成事件**（只由 mousedown/keyup 等触发），**程序化选区与拖选过程中的 `selectionchange` 不走它** → 曾出现「设了选区但自绘高亮不渲染」；补 `document.addEventListener('selectionchange')`（`document.activeElement === 编辑器` 时 `syncCaret()`），拖选实时跟随。**教训：①凡是给文字层叠加几何，几何来源（Range rect）的粒度/高度不一定等于行盒——先按行归一化再画；②「一行一块」比「逐段画」既快又不会露缝；③自绘反馈的事件源要用原生的（selectionchange），React 合成事件覆盖不到拖选/程序化路径。**
- 2026-09 **事故 2（同一天第二次）：用 CDP 截图验证时又覆盖了用户标签 → 从平台「收藏」取回原文**：为了给用户截图验证自绘选区，我跑了会往编辑器灌 SQL 的脚本（**即使只是「截个图」也会触发 debounce 自动保存**），把用户会话 `session-7b05d3de-…` 里从收藏打开的「地点供应商入库比对」标签（`sql/5.sql`）覆盖成了测试用的 dim_shop SQL（该会话另外 3 个标签内容完好）。**恢复路径**：该标签来自收藏（`params/5.json` 有 `__collectId: 998`），用 `olap.collect.tree` RPC 从收藏树里按 id 取回 `querySql`（4252 B / 88 行）写回 `sql/5.sql` ✓。**教训：①「只是截图/只是读一下」的脚本一旦 `setSelectionRange` 或写 `value` 就会落盘——凡是要碰真实编辑器（哪怕只读 DOM 之外的任何交互）都必须先把 `~/.yh-olap/workspace` 移开；②从收藏打开的标签是「可恢复」的：`params/*.json` 的 `__collectId` + `olap.collect.tree` 就是它的权威副本，恢复优先走这条路；③用户标签一旦被覆盖，先备份现状（`*.overwritten-by-test` 之类）再写入，别丢第二份证据。**

- 2026-09 **选区高亮改为「自绘」（第二刀）：div 与 textarea 的边界折行差异是双向的，靠宽度补偿治不干净**：上一版按 textarea 真实断行渲染后，用户实测在**他的机器/宽度**下仍有错位。用户给出的只读诊断是关键：`CW=997 OW=1005 SBW=8 LEN=3849 taRows=85 ovRows=86 brs=7 hiddenMirror=85 scrollMirror=85 scrollOW=85` —— 镜像（含带滚动条两种）与真实 textarea **完全一致（85）**，但覆盖层多了 1 行（86）：镜像里「刚好放得下」的那一段，在 **div 覆盖层**里因亚像素差异多折了一行。我用 `--force-device-scale-factor=2`（真 DPR=2，`deviceScaleFactor` 模拟不改字形取整）复现到 0.5px 级差异；给覆盖层加 4px 宽度余量后，**另一档宽度（cw=457）反而少折一行**（余量抵消了之前「覆盖层多折一行恰好补上镜像少算」的巧合）→ 结论：**同宽 div 与 textarea 在边界行的折行差异双向存在，任何固定宽度补偿都只能按下葫芦浮起瓢**。**最终修法**：不再依赖原生选区底色——`measureRectsIn(hl-inner, [{start,end}], -1)` 按覆盖层实测矩形自绘高亮（`measureRectsIn` 内部只发一次 Range + getClientRects，整篇选区一次拿回所有行矩形），`<style>` 加 `.yh-olap-selhit{position:absolute;background:rgba(122,170,255,.30);z-index:-1}`；原生选区**仅在量到矩形时**透明（textarea class `yh-selown` + `.yh-olap-input.yh-selown::selection{background:transparent}`，量不到则回落原生高亮，不会出现「完全没高亮」）；`syncCaret` 顺手同步 `selRange{s,e}` 并给 textarea 挂 `onSelect`（拖选过程中 `select` 事件持续触发）；折叠选区（光标）直接 return，零开销；矩形坐标相对 hl-inner，随内容滚动，无需 scroll 依赖。**实测（CDP 真实 app）：换行一致性 30/30（DPR1；DPR2 仅 1 档 0.5px 取整）、全选截图逐行像素扫描「高亮右缘之外仍有文字」= 0 行；4KB SQL 选区测量一次即回、光标态零开销。经验：①「所见 vs 可交互」两层渲染出现几何差异时，与其把两层对齐做到 100%（不可能，字形取整是双向随机），不如让**可见反馈跟着可见文字走**（自绘），把无法对齐的部分留给不可见的交互层；②任何「固定像素补偿」都要同时在多档宽度验证，否则只是把失败点从 A 档挪到 B 档；③让用户跑只读诊断（返回一行字符串 + `copy()`）比反复猜快得多——镜像/真实 textarea/覆盖层三者行数一比就定位到层。**

- 2026-09 **编辑器选区/光标与所见文字整段错位 —— 覆盖层改按 textarea 真实断行渲染（治本）**：用户报「全选时选中的显示跟实际文字位置对不上」。根因实测：编辑器是「透明 textarea 在上、彩色文字覆盖层在下」两层，选区/光标由 textarea 画、文字由覆盖层画，而 **Chrome 的 textarea 软换行与普通块（div/pre）换行不是同一套算法** —— CJK/引号/空格贴着换行宽时会差 1~2 行。用用户的 3.sql 扫 101 档面板宽度：95 档一致、6 档分叉（922/944/1021/1406/1483/1934px，差 19.8~40.3px），分叉首行正是截图里开始露字的第 110 行。已排除：字体/padding/宽度不一致、滚动条重复扣宽（按 -8px 改反而 47/101 档错位）、`word-break`/`overflow-wrap`/`line-break` 各组合、`will-change`/transform 合成层、外层 `pre`↔`div`（真实页面沙箱里两者表现完全一致）。**修法（用户选定的「彻底」方案）**：①`measureSoftBreaks(sql, ta)` 用与 textarea 同字体同宽的隐藏镜像 textarea，按「行首前缀」二分出每个视觉行边界（`rows(prefix)` 单调），按「宽度+行文本」缓存、编辑时只重算改动行；②`highlight(sql, engine, breaks)` 在断点处插 `<br>`（`<br>` 不带文本 → `textContent` 不变，查找高亮的 Range 偏移映射不受影响），可见文字 = textarea 真实排版 → 选区/光标/点击落点全部对齐；③行号 gutter 与当前行高亮改用实测断点（`rowsFromBreaks`/`physRowOfOffset`）替代字符宽估算。**过程中踩的三个坑（都已修）**：①按「从上次断点切片」二分测行数会少算（实测逐段 177 行 vs 整段 218 行）—— 必须始终以行首前缀测量；②行边界 `min{k: rows(prefix k)=r+1}` 是「第 r+1 行起点」，第 r 行应结束于 `k-1`（差一个字符 → 每行多折一行、覆盖层反而更高）；③SQL 以 `\n` 结尾时 textarea 多显示一个空行（光标行），覆盖层要补一个尾部 `<br>`，否则整体永远差 20.15px。**实测（CDP 真实 app 内，30 档宽度 900~2000px）：`ta.scrollHeight === round(覆盖层高度)` 30/30 且 `textContent === textarea.value`；用户 3.sql 在 1400px 档 taH 1007 == ovH 1006.9，全选截图逐行像素扫描无「文字露在选区外」；性能：4KB SQL 首次测量 51ms、宽度变化全量重算 27ms（按行缓存，编辑只重算改动行）。**
- 2026-09 **事故与教训：用 headless 驱动真实 5175 前端验证 → 覆盖/删除了用户工作区标签文件**：为在真实 app 内验证上面的修复，我用 CDP 连真实 5175 实例并在编辑器里灌 SQL，**前端自动保存把 `~/.yh-olap/workspace/session-808af8e3-…/sql/` 的 1.sql/2.sql/3.sql 覆盖与删除**（事后只剩被覆盖的 1.sql）。恢复情况：2.sql 全文凭早前读取还原；3.sql 只找回原文件第 95 行起的后半段（前 94 行在平台查询历史/下载工单/DSH session 日志里都搜不到，无法找回）；1.sql 原 12B 内容未知。**教训（已写进验证流程）**：①验证编辑器这类会自动落盘的功能，**不要用 headless 驱动真实实例的编辑器** —— 优先用「在页面里新建孤立 DOM 节点」的沙箱（不碰 store、不触发 autosave）；②确需驱动真实编辑器时，先把 `~/.yh-olap/workspace` 整体移开（`mv workspace workspace.hold`，测完还原）；③调试代码必须在函数作用域内 —— 曾把引用函数内局部变量 `s` 的调试块写在函数外，直接 `s is not defined` 导致插件整体加载失败（页面只剩 "Failed to load plugins"）。

- 2026-09 **下载文件名统一为 `olap-<引擎>-<id><服务端后缀>`（先实测服务端到底给不给名字）**：用户报「下载文件名有问题，接口本身应该提供文件名，不要再自己定义」，要求先核实。直连抓三个下载接口的响应头：`/download/olapResult/{requestId}`、magpie `/open/api/downloadToExcel`（带不带 `&fileName=` 结果一样）、`/download/olapResultSimple/{requestId}` 的 `Content-Disposition` **全是下载时刻的纯毫秒时间戳**（`1789032548310.xlsx`，每次请求重新生成）；带 `-- 标题: …` 的 SQL（工单 146079）服务端也不采用 → **前提不成立：服务端只提供后缀，不提供有意义的名字**。按用户随后给的口径改：①host 新增 `downloadName(engine,id,serverName)`，统一 `olap-<引擎名>-<id><从服务端 CD 名取的后缀>`（引擎名与面板一致 hive/impala/ck/doris，后缀不硬编码）；快速下载 = `olap-<引擎>-<查询 requestId>`、下载页 = `olap-<引擎>-<工单 id>`；删掉原「仅 `^\d+\.xlsx$` 才覆盖 + 从 `-- 标题:`/日期派生」的三份重复逻辑与 magpie 的 `fileName` 参数（实测服务端忽略）。②**顺带修一个真 bug**：文件未生成/失败时服务端返回 JSON 错误体且无 Content-Disposition，原 binary 路径照收不误 → 浏览器落盘 `download.xlsx`、内容是 `文件生成状态未正确完成【3】`；`req` 的 binary 分支加 content-type 含 json 即返回 `{ok:false,error:<服务端 message>}`，客户端走失败 toast、不落盘。**RPC 实测：快速下载就绪行 → `olap-impala-1422242b-…-756089.xlsx`（真 xlsx，`PK` 头）、同一 requestId 借 engine=1 → `olap-hive-…xlsx`（验引擎名映射）、未就绪行 → `ok:false 文件生成状态未正确完成【3】`、下载页工单 146118 → `olap-impala-146118.xlsx`（2.37MB 真 xlsx）。经验：①改「服务端应该给 X」前先直连抓响应头——这里服务端给的确实是时间戳，自造前缀不是多余而是必需；②命名规则集中到一个 helper，别再像原来那样三处各写一套日期/标题逻辑；③下载类接口必须校验 content-type，JSON 错误体混进二进制路径会产生「后缀对、内容是报错」的假文件。`olap.history.download` 没有任何调用方（死代码），保持未改。**
- 2026-09 **知识库从 kb/sqlkb 切到 `@sidleo3/dsh-wiki`（llm-wiki），面板「知识库」tab 下线**：用户放弃 kb/sqlkb，改用 dsh-wiki。改动：①`profile/package.template.json` 的依赖与 bundles 把 `@sidleo3/dsh-sqlkb@0.1.6` 换成 `@sidleo3/dsh-wiki@0.2.0`（模型工具从 `sqlkb_*` 换成 `wiki_*`；bundle 目录仍由 `~/.agents/wiki-registry.json` 的 active 解析，与主实例共享同一份知识库，profile 无需配 dataDir）；②yh-data preset 的 persona / preset.yml / SKILL.md 把 sqlkb 检索流程改写为 wiki 流程（`wiki_list` → `wiki_search` → `wiki_get` 读表+口径，写入前先 `wiki_rules` 看门控、口径类需用户确认后带 confirmed）；③**面板「知识库」tab 整块删除**（用户选定：知识库只在模型侧用 `wiki_*` 工具，不在面板浏览）——client 删 `SqlkbTree` 组件、`st.kb` 状态、`leftTab==='sqlkb'` 分支与 `.yh-ws-kbg/.yh-ws-back` 专属 CSS，左栏回到「库表/收藏」两 tab；host 删 `ws.sqlkb.{list,get}` + `ws.kb.{tree,list,get,search}` 六个 RPC 与 `SQLKB_ROOT/KB_ROOT/parseSqlkbFm/parseSqlkbFile/findKbFile`。**验证：`node --check host.js` + client parse 通过，`profile/ preset/ plugin/ start.sh stop.sh` 全目录 grep 无 sqlkb/ws.kb 残留；`dsh --profile yh-olap --dump-config` 组合树含 `@sidleo3/dsh-wiki` 的 wiki-registry 行、无 sqlkb；重启 5175 实例（pnpm +1 −1，profile node_modules 只剩 dsh-wiki、dsh-sqlkb 已删）后启动无插件树错误；在该 profile 目录 in-process 用真实 config 跑插件的 `apply()` —— 注册 13 个 `wiki_*` 工具且 `wiki_list` 真读到「永辉」bundle（工具环境 2 条 + 永辉/sql/computations 等）；CDP headless 打开 5175 实测左栏 `tabs=["库表","收藏"]`、页面无「知识库」、编辑器与三列布局正常、console 无 error/warning。经验：①删功能要连「读取侧 RPC + 客户端组件 + 状态 + 专属 CSS」一起清，只删 UI 会留死 RPC 与死样式；②换知识库插件要同步三层（profile bundle / preset 指令 / 面板入口），漏一层就出现「模型查得到但面板还留旧入口」；③验证「模型工具是否真在本 profile 生效」最省事的办法是在该 profile 目录里按插件真实 config 跑一次 `apply()` mock（工具名 + 真实 bundle 读取一并验到），不必起会话调模型。**
- 2026-09 **补全列表排序：匹配度优先 + 关键字优先于字段（`complPrefer` 字段上下文改 `['k','col','f']`，`rankMatches` 改 `sc` 先比）**：用户要求「关键字的优先级应该高于字段」，随后在权衡两种实现后选择「匹配度优先」。两步改动：①`rankMatches` 默认 `kindRank` 本就是 关键字(0) < 字段(4)，但 `complPrefer` 在 `select/where/on/having/group by/order by/set` 之后返回 `['col','f']` 把字段提到最前 → 关键字反被压到字段之下；改为 `['k','col','f']`（`from/join` 的 `['sch','tbl']` **保持不动**，那里关键字本来就在字段前）。②排序从「类型 > 匹配度」改为「**匹配度 > 类型** > 长度 > 名称」（`a.sc - b.sc || a.kr - b.kr || …`）——只改①会带来副作用：类型优先级压过匹配度时，1 个字符前缀（如 `s`）会被海量「包含命中(sc=2)」的关键字占满 40 条截断窗口，把「前缀命中(sc=1)」的字段挤到不可见；改②后强匹配永远先出，同档次内仍按类型排。**验证：把 `rankMatches`/`complPrefer` 两个真实函数从 client.js 抽出来在 node 跑 A/B（比在浏览器里制造上下文快得多）——select/from/无上下文三种语境下「关键字高于字段」且「字段都在 40 条窗口内」（select+`s`: 关键字#0、字段#12；select+`sh`: 关键字#0、字段#1）；CDP live 实测弹层头部 `set sum semi show stats select schemas … serdeproperties`（14 条前缀命中关键字）后紧跟 `shop_id[门店编码] shop_sts …` 字段，两者同屏。经验：①改排序规则先把纯函数抽出来跑 A/B；②「类型优先 + 列表截断上限」叠加必然出现「弱匹配同类型把强匹配其他类型挤出窗口」，优先级一旦置顶就要连带检查可见性——匹配度优先是这类冲突的通用解；③权衡不清时先实现用户字面要求、把实测副作用数据摆出来再让其定夺，比自行扩大改动范围稳妥。**
- 2026-09 **当前行背景条左移假缩进修复（`.yh-olap-curline` left/right −12px → +12px，CDP 实测 delta=0）**：用户实测「当前行背景色显示太靠前，会让我误以为前面还有一个空格」。根因：**绝对定位子元素的包含块是父级 `hl-inner` 的 padding-box**（不是 content-box），而文字从 `hl-inner`/`textarea` 的 `padding:10px 12px` 之后才开始 —— 原值 `left:-12px;right:-12px` 相对 padding-box 再外扩 12px，实际色条左缘落在文字左缘**再往左 24px**（右缘同理溢出代码层 12px）。CDP 实测修复前：`curlineLeft=289` vs 文字首列 `313`（dLeft=−24）。修复 `left:12px;right:12px` 后实测：`firstCharLeft=313 / curlineLeft=313 / curlineRight=1126 = inner 内容右缘`，`dLeft=dRight=0`，色条高度 20.14 ≈ `line-height 20.15`；截图确认色条与文本列左对齐、不再向左突出。**经验：给文字层叠加几何（高亮/背景/标记）时，绝对定位坐标要按「包含块 = padding-box」换算——想对齐 content-box 就必须补上 padding，正负号写反就是"假缩进"这类误导性很强的视觉 bug；像素级判据用 `Range.getBoundingClientRect()` 取文字真实左缘对比，别靠目测。**
- 2026-09 **适配新版 DSH（0.1.5-rc.1）——「打开不显示 OLAP 界面」根因修复**：用户更新 DSH 后工作站只剩会话区、OLAP 面板整块消失。CDP 实测（`.yh-olap*` 元素 0 个但 `style[data-yh-olap-style]` 在 → 插件 apply 成功、只是面板没挂上）定位到 5 处契约变更：①**`details` slot 被删除**，右列改名 `rightbar`（由 `dsh-client-ui-sidebar-right` 的 dock 占用），中心列变成 keyed slot `main`——`slots.inject('details')` 永不触发 = 面板从不渲染（直接根因）；②**`layout.openDetails()` 删除**，新 API `openRightbar(track, fullscreen)`（`typeof` 判空后原调用静默跳过，无任何报错）；③frame 状态属性 `data-details-collapsed` → `data-rightbar-collapsed`；④`connectWorkspace`/`startSession` 从 `workspaces` 服务移到新的 `uiWorkspace` 服务（`sessions.list`/`workspaces.list` 快照仍在，未变）；⑤默认 preset `yh-data` 挂载失败：新版 `@deepseek-ai/dsh-persona` config 必填 **`prefix`**（旧版叫 `text`）→ `SessionCreateError`，新会话建不出来。**修复**：slot 注册改 `rightbar`（沿用 `priority:-1`；SlotCore 语义为 priority 升序取首个 = **低者胜**，-1 遮蔽占用者 0）；`openRightbar(true,false)` 替换 4 处 `openDetails()`；`wsFrameEl()` 统一 frame 定位；workspace 新建改走 `ctx.get('uiWorkspace')`；preset `text:` → `prefix:`。**顺带修掉一个潜伏 bug**：frame 定位原靠 `[data-sidebar-collapsed],[data-details-collapsed],[data-dragging]` 反查，**「侧栏展开 + 右列展开 + 未拖动」时三个属性全不存在** → frame 找不到 → `.yh-ws-frame` 打不上 → 三列重排/分隔条整体失效（1680 宽视口实测 OLAP 落回最右列）；改为优先用稳定的 `[data-shell-overlay]` 反推父元素，属性查询仅兜底。**CDP 实测（748/1680 两档视口）：面板回到中列（side 56 + OLAP 1083 + 会话 541，分隔条 left=1138.66 与列边界 1139 对齐）；切会话面板跟随并恢复该会话标签/SQL（空白 Tab1 → 哈喽 恢复「销售」+ 原 SQL）；点新建会话得纯新 1 空 Tab1 且无 SessionCreateError；知识库 tab 正常列出表/示例/坑点；无 console 异常。经验：①DSH 大版本升级先按「slot 名 + service face + DOM 状态属性」三类契约逐项对照新包源码，`typeof x === 'function'` 的兼容写法会把「API 被删」变成静默失效，最难查；②靠状态属性反查框架根元素不可靠——状态属性只在某个状态下存在，要挑恒在的结构锚点（如 slot 容器）反推；③插件 slot/service 契约以新版包 `lib/client.js` 源码为准，Inspect service catalog 只列公开面（`sessions.list` 就不在其中但真实存在）。**
- 2026-09 **收藏重复打开标签修复（同一收藏只允许一个标签，刷新后仍生效）**：用户实测"从收藏打开 1 个标签，再点击收藏又打开了一个相同的标签"。修复：①**点击收藏去重**——CollectNode 点击时先查 `st.tabs.find(t => t.collectId === node.id)`，已存在则直接 `activeTab` 切换 + toast「已切换到 xxx」，不新建（与 wsOpenSqlFile 的同源激活模式一致）；②**collectId 随自动保存持久化**——仅凭内存 dedup 不够：刷新后 wsLoad 恢复的标签对象不带 collectId（原 wsApplyToStore 重建时丢弃），再点收藏仍会重复。故 params 落盘时追加 `__collectId`（CollectNode 立即落盘 + wsPersistAll 两条写路径），wsLoad 解析 `__collectId` 回填、wsApplyToStore 重建标签时携带 `collectId`，恢复的收藏标签也参与去重（且收藏保存/删除的 collectId 判断在刷新后同样生效）。**CDP 实测：点收藏→开 1 个标签；再点同收藏→不新建、激活已有（tab 数不变）；刷新→收藏标签恢复且再点同收藏仍不重复。经验：跨刷新去重/归属类元数据（如 collectId）必须随自动保存持久化并在恢复链路（wsLoad→wsApplyToStore）完整回填，只做运行时去重会在刷新后失效；标签归属类信息放进 params 的 `__xxx` 字段与 `__name` 同理。**
- 2026-09 **查找高亮位置错位修复（实测定位取代字符宽估算，CDP 逐像素验证）**：用户报"查找时高亮的位置不对"。用 CDP headless 在真实页面对照 `Range.getBoundingClientRect` 实测每个高亮矩形 vs 高亮层文字真实位置：①**CJK 漂移**——模型按 `2×charW` 估算中文字宽，真实字形约 1.66×（ui-monospace 实测），短行无换行也逐字偏右（6 个中文字符后偏 16px）；②**软换行完全错位**——模型按"任意字符处折行"计算，而浏览器 `pre-wrap + word-break:normal + overflow-wrap:break-word` 只在断点处折行、超长 token 用 break-word 打断，长行/长 token 高亮落在完全错误的行和 x（实测 dL=697）；③另发现 `.yh-olap-hl-inner` 的 `overflow-wrap:normal` 与 textarea 的 `break-word` 不一致 → 覆盖层文字与真实文本在超长 token 上换行不同（顺带修复为 `overflow-wrap:break-word` 与 textarea 对齐）。**修复：弃用按字符宽估算定位，改为渲染后 `useLayoutEffect` 用 DOM `Range.getClientRects()` 实测每个匹配矩形**——`measureRectsIn(inner, matches, curIdx)` 走 hl-inner 的文本节点建偏移表，每个匹配建 Range 取真实矩形（跨行匹配自动分段、`cur` 标记当前项），结果存 `findRects` state 驱动高亮 div；deps=[fb.open,q,caseS,word,re,pos,sql,resizeTick]（q/开关/SQL/宽度变化时重测，deps 不变不会重入）；ResizeObserver 顺带 `setResizeTick` 触发重测；实测失败回退旧 matchRects 保证高亮不消失；`scrollPosIntoView`（Enter 导航滚动）也改用实测矩形，回退 cursorXY。**CDP 实测 300 匹配/307 矩形（7 个跨折行）全部 dL=dT=0，Enter 导航 cur 高亮 1/3→2/3→3/3 正确跟随。经验：①浏览器文本布局（断行规则/CJK 字宽）无法用字符宽累加模型复刻——凡是"高亮/标记盖在文本上"的定位，直接对同层文本节点建 Range 取 getClientRects 即可像素级精确，别再手推几何；②改"渲染时按状态算位置"为"渲染后实测"要放 useLayoutEffect + state 驱动，并保证 effect deps 稳定避免 setState 死循环；③textarea 与覆盖层的 wrap 相关 CSS（word-break/overflow-wrap/white-space）必须逐项一致，否则两层文字换行点分叉。**
- 2026-09 **「有结果但按钮卡停止」根治（CDP 实测复现+修复，扩展上一轮 45s 看门狗方案）**：用户实测跑慢 SQL（如 `sdt between '20260709' and '2060907'` 全表扫描）"结果已出但停止键不恢复、不跳结果页"。先用 `yh_bigdata olap sql submit/check/result/log` 实测后端契约：`checkState` 完成即返 `finish:'ok'`+`errMsg:'请查看结果'`（0 行也是 ok）；`getLogResult` **永远** `finish:'run'`、执行中 data=`sql执行中,请稍后....` 完成后变空；`getSqlResult` 的 `isReady` 在 finish=ok 后 ~2s 内翻 'ok'。再 CDP headless 复现两种卡死：①**切标签即永久卡死**——原 pollRun 的 alive()/settle/看门狗全部实时取 `activeTab(st)`，用户运行中切到其他标签 → 三循环全退、看门狗放弃 → 切回后按钮永停「停止」；②**45s 看门狗误杀长查询**——日志"sql执行中,请稍后...."命中关键词「稍后」→ 仍在执行/慢但成功的查询 46s 处被标 `IMPALAERROR:[执行超时或失败]`。修复（client `pollRun` 重构）：①**runTab 绑定**——doRun 把发起时的 tab 对象传进 pollRun，alive/settle/三循环/看门狗全部用它，切标签不再中断轮询收尾（CDP 实测切走 45s+ 再切回：轮询继续、完成后结果已就绪）；②**成功=finish==='ok'**——后端完成信号即成功（0 行也成功收尾切结果页），原要求 `result.list.length>0` 导致 0 行永远走不到成功分支；结果未就绪时最多等 10s 物化宽限再收尾；③**看门狗改停滞检测**——任一轮询 RPC 成功即 `lastTick` 续命（30s 复检），只有三循环持续 >20s 无成功响应（RPC 全挂）才先做最终 checkState 再判定，长查询不再被误杀；④**结果补拉 fillResult**——settle 成功但结果仍在物化时继续拉 getSqlResult（≤120s），晚到的 rows 补进结果页（原 resLoop 只拉 30s 就死，晚到结果被吞成 0 行）；⑤doRun 提交响应缺 executeId 也失败收尾（否则 running 永真）；⑥logLoop 去掉 RPC 失败时的双重复排（catch+兜底各排一次会指数翻倍）。**CDP mock 实测 6/6 PASS：fast 有行→成功切结果、0 行→立即成功、65s 慢查询→45s 不误杀 65s 收尾、切标签→轮询不中断回来自动收尾、60s 中文错误→失败收尾带 IMPALAERROR、手动停止→复位。经验：①轮询循环的"当前标签"必须绑定到发起时的 tab 对象，任何实时取 activeTab 的判定都会被切标签打断；②看门狗要区分"查询慢"（循环健康）与"轮询死"（RPC 全挂）——用最近成功 RPC 时间戳判定，固定墙钟强杀 + 日志关键词误判（'稍后'是进度文案）必然误杀慢查询；③后端完成信号是 checkState.finish==='ok'，不是结果行数——0 行也是成功。**
- 2026-09 **库表清单右键复制库名/表名/字段名**：新功能——左栏「库表」树右键：库行→「复制库名」、表行→「复制库名/复制表名」、字段行→「复制库名/复制表名/复制字段名」，点击即 `copyToClipboard` + toast「已复制 xxx」。实现：`st.schemaMenu` 状态（kind/schema/table/field/x/y）+ `SchemaMenu` 组件（复用 `.yh-olap-cmenu/.yh-olap-citem` 样式与 CollectMenu 的 document mousedown 关闭模式），SchemaTree 三级行分别挂 onContextMenu（字段行直接挂 div，库/表行沿用 CollectNode 的包一层 div 模式）。CDP 实测：右键 sjyy→复制库名=sjyy、表 sjyy_dim_shop→复制表名=sjyy_dim_shop、字段 order_setl_sys→复制字段名=order_setl_sys，复制后菜单自动关闭。
- 2026-09 **执行报错后按钮卡「停止」不恢复（中文错误如 `IMPALAERROR:[资源繁忙,等待资源较长,请稍后重试]`）**：用户实测执行报错后「执行/停止」按钮一直停留在「停止」。根因（client `pollRun` 三循环）：①`stateLoop` 错误判定正则只认英文关键词（`/Error|Exception|Analysis|failed|.../`），后端 `checkState` 对「资源繁忙」类**中文错误**返回 `finish:'run'`+中文 `errMsg` 时永不命中 → 只能等 80 轮(40s)兜底；②兜底分支 `!gotError && !cur.errMsg` 才收尾、`errMsg` 被填充时**裸 return 不复位** → 若 RPC 正常可拖 40s 才复位（用户感知卡死），若中途 RPC 异常则**永不复位**；③三个轮询 loop 的 `.catch(()=>{})` **静默死亡不续排**（checkState 网络异常 → stateLoop 死、running 永 true）；④`logLoop` 收到后端 error 字段错误只追加日志、**永不判定收尾**。修复：①新增统一收尾函数 `settle(asError,msg)`——所有失败/成功路径收尾都走它（running=false + finish/errMsg/log 拼接 IMPALAERROR + bottomTab + bump）；②`stateLoop` 错误判定放宽：`errMsg` 命中英文错误特征 **或 `errMsg` 非空且 `finish!=='ok'`**（中文错误立即收，不再等 40s）；③80 轮兜底**无条件收尾**（有 errMsg/错误日志按失败，否则 0 行成功），杜绝裸 return 卡死；④三个 loop 的 `.catch` 改为**续排下一次**（RPC 异常不再静默死亡）；⑤`logLoop` 收到 error 字段错误 → 追加一次 + `settle(true)` 立即收尾（错误日志=执行已失败）；⑥新增 **45s 总超时看门狗**——到点仍 running 则按日志错误特征收尾，任何遗漏路径的最后保障。**CDP mock 实测：A.state 中文 errMsg→2.5s 内按钮复位、B.log.error 中文→复位、C.成功有结果→正常切结果页(回归通过)、E.手动停止→复位、F.RPC 全 reject→45s 看门狗兜底复位，8/8 PASS。经验：轮询驱动的 running 状态必须保证"任何路径最终都能收尾"——错误判定正则要覆盖后端真实错误形态（中文！）、loop 异常不能静默死亡、要有独立看门狗兜底；判定收尾与竞态(成功/失败/停止)要收敛到一个 settle 入口避免各分支自行复位遗漏。**

- 2026-09 **编辑器查找替换（VSCode 风格）**：新功能——编辑器右上角查找框（Cmd/Ctrl+F 打开、Cmd/Ctrl+Alt+F / Ctrl+H 展开替换行、工具栏「查找」按钮、Esc 关闭回焦编辑器）。查找：实时匹配计数 `n/m`/无结果/无效正则、全部匹配高亮 + 当前匹配强高亮（hl-inner 内 `z-index:-1` 叠文字下，与 curline/mcur 同几何）、Enter/Shift+Enter 上下导航并滚入视区、打开时预填编辑器选中文本；选项 Aa 区分大小写 / ab 全字 / .* 正则（支持 `$1` 替换分组、零宽匹配 lastIndex++ 防死循环、非法正则红框）。替换：当前/全部，**写入走 `execCommand('insertText')` 进浏览器 undo 栈（Cmd+Z 一步撤销全部替换），失败回退 `setTaValue`**；替换后焦点还原到最近使用的输入框（fbFocusRef）。**实现要点：①匹配高亮矩形 `matchRects` 用 `cursorXY` 起点锚定 + `chWidth` 逐字符增量推进（`used+cw>uw` 折行与 cursorXY 判定一致），遇 `\n` 重新锚定防累计误差；②状态 `st.findBox` 进 per-session store（makeStore）→ 换 tab 保留、切会话干净、工具栏按钮与 Editor 共享；匹配在渲染体纯计算派生（不 emit），当前匹配由 `pos` 锚点（首个 start>=pos，回绕）决定；③替换前校验 `ta.value === sql`（渲染闭包 sql 与 DOM 一致，防模型并发 write 被旧内容覆盖），写入统一过 onInput 路径 → 自动落盘 wsScheduleSave + bump 上传 host（模型 state 立即可读，遵循「写入侧三件事齐备」教训）；④输入框 Esc/Enter 均需判断 `e.nativeEvent.isComposing`（输入法组词中 Enter 是确认候选）；⑤渲染上限：高亮前 500 匹配、扫描 guard 2 万/结果 5 千，防大 SQL 卡顿。**
- 2026-09 **olap/mode 事件污染 session 日志 → 历史会话加载报错 + 日志修复事故**：用户实测历史会话载入报 `SessionFormatUnsupportedError: session contains event type "olap/mode" unknown to this harness and not marked ignorable`。根因：host 用 `session.append('olap/mode')` 持久化 OLAP 模式（仿 plan/mode），但框架 session-persistence 读校验只认 `KNOWN_SESSION_EVENT_TYPES`（`@deepseek-ai/dsh-session` 的固定集合），append API 无法标记 `ignorable` → 含该事件的历史日志全部拒读。修复：①模式状态改存 host 内存 Map（sid→bool），**不再 append 事件**；②新增 `olap.mode.get` RPC，客户端 OlapChip 改轮询它；③sessionProjections 注册保留（旧日志历史事件仍驱动，无害）。**数据修复事故教训**：清理旧日志里的 olap/mode 行时用 `zstd -dc | grep -v | zstd -o` 整体重压——**破坏了 DSH 专有的分帧格式**（原文件每帧是写入批次，首帧必须恰好一行 header；整体重压成单帧/少帧后 `assertZstdHeaderFrame` 拒读）→ 实例启动崩溃 `corrupt Zstandard session log`。且备份脚本 `basename $(dirname)` 命名冲突导致 14 个备份互相覆盖只剩 1 个。**正确修复**：用 node `zstdCompressSync` 按"首帧=header 单行 + 后续每 ~200 行一帧"重建（21 个文件全部恢复可读）。**教训：①插件绝不往 session log append 框架不知道的自定义事件——UI 状态存内存/投影，别污染持久化日志；②DSH 的 session.jsonl.zstd 是多帧格式（首帧必须单行 header），任何改写必须保持分帧结构，禁用整文件重压；③批处理备份文件名必须含唯一 id，不能用公共目录名。**
- 2026-09 **标签恢复慢（刷新后几秒才出）+ 重启后操作慢**：用户实测"刷新后只有 1 个空 Tab1，几秒后标签才恢复"。两个延迟源：①`wsAutoOpenFirst` 首次 setTimeout **2.5s** + interval 1.2s → 会话 current 确立慢 → OlapPanel sid 就绪晚 → 恢复晚（改 300ms + 500ms）；②`wsLoad` **串行读文件**（每文件 sql/params/note 3 RPC 依次等，N 文件 = 3N 串行往返）→ 改 `Promise.all` 并行（文件间并行 + 每文件 3 read 并行），本地磁盘 <500ms。另外"重启后操作慢"根因是**浏览器开多个 5175 标签**（每面板 800ms sync + 1.5s poll + 上传叠加吃 CPU，Edge 32% CPU + 34 连接）——关到 1 个标签即正常，非代码 bug。**经验：①初始化链路里的 setTimeout 首次延迟（2.5s）要警惕——它拖慢一切依赖会话确立的后续；②本地多文件恢复必须 Promise.all 并行，串行 RPC 是"本地也慢"的隐藏元凶；③"界面卡顿/操作慢"先查浏览器标签数和进程 CPU（多个面板实例高频轮询），别急着改代码。**
- 2026-09 **面板状态上传慢 → 模型 state 读到旧/空 SQL（"不顺畅"）**（会话 f5bdf152 实测）：用户刷新后引用 `@#1 L13` 让模型改，模型 state 反复读到 `sql=""`（3 次空），被迫 grep/bash 在磁盘找 SQL，拖很久才成功。根因：`schedulePanelUpload` 走 `uploadThrottle` **固定延迟 1s** 才上传 host（首次也不立即），用户刚编辑完立即让模型 state 时 host lastState 还是旧/空；高频操作会不断推迟 pending。修复：①`uploadThrottle` 延迟 1s → **300ms**；②OlapPanel 的 **1.5s poll interval 顺带 uploadPanelState** —— host lastState 常新（≤1.5s 滞后），任何时刻模型 state 读到最新。**经验：客户端状态 → host(模型可读) 的上传不能只靠'变化时 debounce'（有延迟+可被推迟），要有常驻定时兜底上传；上传延迟决定模型 state 的滞后上限，用户'刚操作完就问答'场景要求 ≤1s 级同步。**
- 2026-09 **wsApplyToStore 恢复后不上传 host → 模型 state 读空 SQL**（会话 ab924be4 实测）：全自动保存改造后，用户刷新页面引用 `@#1 L13` 让模型改，模型 state 读到 `sql=""` 报"面板 SQL 为空"无法修改，但用户面板实际有完整多行 SQL。根因：刷新后 wsLoad 异步恢复 → wsApplyToStore 填 SQL 进客户端 store + emitStore（**UI 正常显示**），但**没上传 host** → host lastState 停留在挂载时上传的初始空快照 → 模型 olap state 读空。修复：wsApplyToStore 恢复后**立即 uploadPanelState**（绕过 1s 节流、clearTimeout 取消 pending）。**经验（上传三缺一的又一例）：凡 store 内容被整体替换（wsLoad 恢复/重置），必须立即上传 host——只 emitStore 会让 UI 与模型可读状态分叉；挂载上传初始、恢复后再上传全量，两段都要。**
- 2026-09 **全自动保存模型（去掉工作区概念）**：用户需求变更——工作区不需要了，所有标签默认全自动保存（含收藏打开的标签），刷新/重启打开会话默认加载所有标签，关闭标签即删除本地文件。实现：①所有标签创建点 `autoSave=true` + `wsFile` 关联（初始 Tab1/加号新建/收藏打开/模型 newTab/newTab 会话重置），每标签对应 `~/.yh-olap/workspace/<sessionId>/sql|params|notes/<id>.*` 一份文件；②新建标签立即落盘（含 `__name`），编辑 debounce 落盘；③关标签（removeTab/右键删除）删 sql/params/note 三件套不确认，收藏标签（collectId）只删本地副本不影响服务端收藏，最后标签关闭后新建空 Tab1 并落盘；④左栏去「工作区」tab（剩库表/收藏/知识库），TabMenu 去「自动保存」开关项，tab 去 ◉ 指示；⑤DSH「新会话」按钮仍重置为纯新空 Tab1（per-session 隔离不变）。WorkspaceTree/wsRefreshTree/wsNewSqlFile 等成死代码保留（不被渲染）。**CDP headless 实测：新建 Tab2+编辑→2.sql/2.json 落盘；刷新→Tab1+Tab2 全恢复；删除 Tab2→文件删、界面剩 Tab1。经验：用 headless Edge `--remote-debugging-port` + CDP WebSocket 可绕过 ego 自动化挂起问题做端到端验证（ego 在本环境连任何页面都会挂起）。**
- 2026-09 **write+lines 区间替换 bug 修复**：用户实测（会话 4a7e5825）引用 L13 让模型改一行，结果**整个编辑器内容被替换成那一行**。根因：`applyCommands` 的 write 分支对 `lines` 沿用了 run 的「裁剪」语义——`s = tab.sql.slice(start-1,end)` **丢弃了模型给的 c.sql** 且把整个 tab.sql 设成裁剪结果。模型意图是"用 c.sql 替换 [start,end] 行区间"。修复：lines 存在时 = `head(区间前) + c.sql(可多行) + tail(区间后)` 拼接替换；run 的裁剪语义（执行选中行）不受影响。**经验：同一参数 lines 在 run(裁剪执行) 与 write(区间替换) 语义不同，不能复用同一段处理代码——write 必须保留 c.sql 作为替换内容，lines 只决定替换范围；区分"裁剪出区间"(run)与"用新内容替换区间"(write)两种操作。**
- 2026-09 **"滚动错位"实为环境问题，非代码 bug（A/B 实测结论）**：用户报"滚动后行号与代码不同步"，曾误判为代码 bug 并做"根治"改动（08491f7）→ 引入回归（行号不滚+空白）→ 回退。后经**受控 A/B 实测**：v1.0.0-baseline 版重启+硬刷新后**滚动正常**；切回 HEAD 版（含右键引用等全部新功能）重启+硬刷新后**也正常**——同一份代码，之前"错位"、之后"正常"，差异只在**浏览器缓存了旧 client.js / 实例未真正重启**。诊断数据佐证：长 SQL 下 `ta_scrollH(2136) == hli_scrollH(2136)`、`transform == scrollTop`、宽度 `ta_contentW(985) == hli 内容宽`，几何全部对齐。**教训：①任何"编辑器几何/滚动"类 bug，动手前必须先做受控 A/B（重启+硬刷新 Cmd+Shift+R 排除缓存）确认是代码还是环境；②ego/headless 连 5175 会挂起，浏览器验证可让用户跑诊断 JS 取真实 DOM 数据（ta_scrollH vs hli_scrollH、clientWidth、scrollbarW）；③实例加载的是启动时磁盘快照，改 client.js 必须重启才生效，浏览器可能缓存需硬刷新——排查"改了没生效/莫名错位"先查这两点。**
- 2026-09 **滚动错位"根治"方案已回退（git revert 08491f7）**：曾尝试用「hl/gutter 高=ta.scrollHeight + curline 用 mirror 实测」根治深滚错位，结果引入回归——行号不滚、滚动后视口外内容全空白。**教训：编辑器高亮/行号的滚动同步是高度耦合的几何系统，改一处会牵动 transform 位移、gutter 滚动、内容高度三者的匹配；不要一次性做"撑高 scrollHeight + mirror 实测"这类大改，应先小步验证。当前回到基线状态（能正常滚动显示；深滚可能轻微差一行的偶发未确认根因）。若再修：先小步改（只动 curline→mirror 实测并实测滚动），确认无回归再继续；改前必须浏览器实测滚动（ego 连 5175 会挂起，需用真实浏览器/CDP 或让用户验证）。**
- 2026-09 **编辑器选中 SQL 右键引用（@olapN:Lx-y 行区间块）**：新功能——在编辑器选中部分 SQL，右键「引用选中 SQL」→ 以 occurrence 块插入聊天框，块展开为 `@olapN:Lx-y`（标签 #N 的第 x~y 行），label 显示 `#N Lx-y 「首行摘要」`（不占上下文、渐进披露）。配套：host `olap state` 支持 `{tabId, lines:[x,y]}` 精确返回该行区间 SQL；OLAP_MODE_GUIDE 已说明该引用格式的读取（state lines）与改写（write lines）方式。**实现要点：行号计算要去掉选区末尾换行（整行选中含 \n 不多算下一行）；插入走与 referTab/@olap 菜单同款 ReferenceInsert（source:'olap'），codec.serialize 展开成纯文本给模型；Editor textarea 此前无 onContextMenu（现有右键都在树/标签上），新增时复用 `.yh-olap-cmenu` 菜单样式 + document mousedown 关闭。**
- 2026-09 **applyCommands 不落盘 → 模型写入的 SQL 刷新丢失**：用户实测"开启自动保存的标签，对话(模型 olap write)写入的 SQL 刷新网页后消失"。根因与上两个 bug 同构——applyCommands 写 `tab.sql` 后只 bump/上传，**没调 `wsScheduleSave`**（手动输入 onInput 每次变更都调），模型写入不落盘。修复：①applyCommands write 分支写 SQL 后 `wsScheduleSave(sid, 200)` 立即落盘；②wsScheduleSave 开关判断从"活动标签 autoSave"改为"**存在任一 autoSave 标签**"（wsPersistAll 实际落所有 autoSave 标签含非活动，原判断在 write 到非活动标签时误拦）。**经验（本条链完整版）：客户端 store 里 `tab.sql` 的任何写入侧必须三件事齐备——`emitStore`(重渲染) + 上传 host panelStates(模型 state 可读) + `wsScheduleSave`(刷新不丢)。本文件先后因缺①③②踩了三个坑：applyCommands 曾缺上传→模型 state 误判、缺落盘→刷新丢失；写入侧统一走一个封装函数（setTabSql）可根治。**
- 2026-09 **applyCommands 不上传状态 → 模型 state 误判"未写入"**（会话 f981f0f8 实测）：上一轮给 write 加"必须 state 验证"指引后，暴露更深 bug——SQL 实际已写入编辑器（用户确认），但模型反复 state 读到 `sql=""` 误判未写入、反复 write/refresh。根因：**applyCommands 的 bump 只 `emitStore` 不上传 host**（OlapPanel 的 bump 才带 schedulePanelUpload），host 的 lastState 停留在 write 前旧快照 → 模型 state 永远读旧值。修复：applyCommands 命令处理后 `needsFlush` 立即 `uploadPanelState`（绕过 1s 节流，clearTimeout 取消 pending）。时序铁证：write→state 间隔 2.4~2.8s（>1.5s poll 周期，排除 poll 延迟）。**经验：改面板状态的两条路径（用户操作/命令 apply）都必须同步上传 host 的 panelStates——凡是"客户端状态 → 模型可读"的桥，任何写入侧漏了上传都会造成模型侧读到陈旧状态；本文件里有两个 bump 需保持一致（OlapPanel L3620 带上传、applyCommands 必须也带）。**
- 2026-09 **olap write "假填入"修复（面板在线检测+写入后验证）**：用户反馈"模型说 SQL 已填入 @olap1 但编辑器没显示"（会话 4634981c 实测）。根因：`olap.write` 只 enqueue 命令进 host 内存队列就返回 `queued`，模型把 queued 当"已填入"回复用户；而命令要等**客户端面板每 1.5s poll 拉取并 apply** 才上屏——若命令滞留无人 poll 的队列（面板未打开/切走/刚刷新/agent 会话与面板跟随会话错位/多 poller 抢队列），UI 永不显示且无任何错误。修复：①write 入队前检测面板在线（lastState 有值=最近上传过状态），不在线返回明确错误；②校验目标标签存在；③返回明确"入队≠已上屏，需 state 验证"；④OLAP 模式指引强制 write 后再次 state 验证 sql 已写入，不符则报告面板未同步。**经验：所有"命令入队→客户端消费"的跨进程操作都不能以入队成功当完成——必须让模型在操作后 state 回读验证；session.jsonl 只记模型侧事件，客户端 poll/UI 行为不在其中，排查"模型说做了但 UI 没变"要靠 host 队列残留探测 + 静态审计时序。**
- 2026-09 代码评审 + P0/P1-P3 优化（git 基线化）：①**git 版本管理**：仓库纳入 git，评审前可运行版打 tag `v1.0.0-baseline`，随时 `git checkout v1.0.0-baseline` 拉回；②**启停安全**：start.sh 后台启动写 PID 文件（`$DSH_HOME/yh-olap.pid`）+ 日志，重复启动检测带 <10s 宽限期；stop.sh 首选 PID 文件精确停止，命令行兜底需 dsh+profile+port 三条件齐备——**修复原 `pgrep -f` 模糊子串匹配误杀无关进程的风险**（曾实证误匹配到无关进程）；③bash3.2 `set -u` 下变量后跟全角字符被吞成 `PORT�` 的 UTF-8 词法 bug：所有 `$VAR` 插值改 `${VAR}`；④**渲染体副作用外移**：`activePanelSid` 赋值移入 `useEffect([sid])`（消费方均用户交互时触发，不晚于原渲染期）；⑤**结果表分页已回退**（git revert c8792d3）：曾实现「加载更多」服务端翻页，实测 `getSqlResult` 的 pageNo/pageSize 分页是**摆设**——服务端最高只返回 200 条，翻页取不到更多，功能无效故整体回退；**经验：OLAP `getSqlResult` 单次最多 200 条且不可翻页，取全量走「全量下载工单」而非分页**；⑥**自绘对话框**：新增 `DlgModal`（`st.dlg` 状态驱动 confirm/input，Esc/遮罩关闭、input 自动聚焦、onOk 可拦截空值），替换全部 7 处 `window.confirm/prompt`（收藏目录/重命名/删除、工作区重命名/删除、账号删除）；⑦**CSS token 收口**：`#4176e6/#e5484d/#fff/rgba-weak` 硬编码收敛为 `:root` 深浅两套 `--yh-ui-brand/-weak/--yh-ui-danger/-weak/--yh-ui-ondark`；⑧client.js 顶部加功能区段索引注释。**经验：react 渲染体禁止副作用；单文件巨石定位难 → 文件头留行号索引；改脚本前先查 bash3.2 对 `$VAR+全角字符` 的解析；依赖服务端 API 能力前先实测其真实行为（文档/猜测 ≠ 实际）。**
- 2026-09 **olap 工具 schema 修复（commandcode 网关 400）**：模型会话实测报 `Invalid schema for function 'olap': ... got 'type: null'`（provider=commandcode）。根因：host.js 里 `olap` 工具 `parameters` 用**裸字段简写**（属性平铺、顶层无 `type`），commandcode 等严格网关不认裸 schema → 400 拒绝整轮模型调用。修复=改成标准 JSON Schema（顶层 `{type:'object', properties:{...}, required:['action']}`，数组 `items` 补子属性），与 sqlkb 可用工具及**上游 dsh-yh-olap 已修复版**完全对齐（上游 installable/src/host.js 有同款注释"裸字段表根上无 type 会被 400 拒绝"）。**经验：模型工具 `parameters` 必须是标准 JSON Schema（根 type:'object'+properties+required），`required` 写在属性内（`required:true`）是无效简写；DSH 自有通道可能容错但 commandcode/OpenAI 兼容网关严格校验——定义工具一律用 defineTool 产出形态。改 host.js 后需重启实例（link: 加载进内存不热更）。**
- 2026-08-28 初始搭建：独立 profile + 工作站插件（vendored yh-olap v1.0.1）+ yh-data preset + 启动脚本。
- 2026-08-30 首启体验修复：preset `tool-todo` 补 `config.allowParallelInProgress`（缺失会导致 preset 挂载失败→建会话失败→卡 hero 页）；客户端无会话时自动 `workspaces.connectWorkspace` 建绑定工作区的会话；start.sh 播种默认工作区（含 workspace.json 不存在时）。**首次打开直接进入工作站（左 OLAP / 右会话），无需手动选工作区。**
- 2026-08-30 布局修复：AppFrame 网格只设 `grid-column` 时 details 被 auto-placement 压进隐含第二行（列高 0、OLAP 不可见）；center/details 补 `grid-row:1` 修复。**验证网格重排必须查列高 + 编辑器是否在视口内 + 实际跑一次 SQL，不能只看 innerText/列宽。**
- 2026-08-30 卡死修复：`SqlkbTree`/`WorkspaceTree` 在渲染函数体内调加载函数（内部 `bump()`→`emitStore()`→同步重渲染→条件仍成立→再调）造成**无限重渲染→浏览器卡死**；加载触发移进 `useEffect`（挂载时跑一次），渲染体只读状态。**规则：任何组件渲染体禁止调用会触发 store emit 的函数，加载一律走 useEffect。**
- 2026-08-30 对话区宽度可调：OLAP/会话区之间自建分隔条 `.yh-ws-divider`（挂 `shell.overlay` 层，该层覆盖全 frame 且子元素 pointer-events:auto）；网格列宽改 `grid-template-columns:var(--yh-ws-cols,默认)`，拖拽把 OLAP 写成像素宽、会话区吸走剩余，持久化 `localStorage['yh_ws_olap_w']`。**不要复用 AppFrame 内建 `[data-side]` 手柄：其 left 按原始列序定位，与重排后边界不符。** start.sh 顺带修 bash3.2 `set -u` 空数组展开报 `unbound variable`。
- 2026-08-30 标签引用中文名修复：右键「引用」原走「纯文本 `@TabN` + textRef 扫描装饰」，框架 textRef 正则只认 `[\w-]+`，中文名永远成不了块且被 lexicon 过滤（还回退成 `Tab4`）。修复：`referTab` 改走**真实 occurrence 块插入**（`conversation.input.for(actx).insertReference()`，label 用标签名任意文本，ref=`olap+id`，提交时 codec.serialize 展开成 `@olapN` 给模型），与 @olap 菜单 pick 同款 ReferenceInsert。**经验：输入框「块」有两种——textRef 扫描装饰（只认 ASCII word）和 occurrence chip（label 任意文本）；要让任意字符标签成块必须用后者。**
- 2026-08-30 OLAP 主页面精简+参数弹窗+表头修正：①删 OLAP 入口按钮（OlapToggle 组件+`.yh-olap-toggle` 样式）与标签栏最大化按钮（`⤢`+`.max` 样式），OLAP 是主页面无需入口/最大化；②参数弹窗参数名 `${xxx}` 作为独立标签放输入框外（`.yh-olap-plabel`），避免输完不知对应参数；③结果表 sticky 表头滚动错位：`.yh-olap-reswrap` padding 8px 使 `top:0` 表头锚错，滚动后 body 行露出在表头下方 8px——`thead th` 的 `top` 统一 `-8px` 抵消 padding，`thTop==wrapTop` 且 `rowAboveHeader==null` 为通过判据。**经验：CSS sticky 表头的 top 相对 padding-box 锚定，容器有 padding 时必须用负 top 抵消。**
- 2026-08-31 OLAP 默认列宽加宽 + 历史会话「加载中…」修复：①默认网格 `1.45fr` → `2fr`（OLAP 占 2/3，@1904 下 1280/640px），拖拽仍可自定义+持久化；②历史会话面板永远转圈根因=**订阅注册晚于 fetch resolve 的竞态**：按钮 onClick 先发 `ws.sessions.list`（本机 4ms 返回），overlay 的订阅 useEffect 首渲染后才注册 → lz 空 → `loading=false` 更新丢失 → 永远「加载中…」；修复=加载收敛进组件内部（useState + 挂载后 useEffect 发起，setState 必达），删 `wsReloadSessions`。**经验：本地 RPC 太快时禁止「全局对象+后注册订阅集合」推状态，加载必须放组件 useEffect（订阅建立之后）；面板状态一律收敛进组件 state。**
- 2026-08-31 sidebar 展开溢出修复 + 左栏 4 标签可见：①**溢出**：聊天区拖到最小 360 时展开 sidebar（56→280 让出 224px），`--yh-ws-cols` 固定像素不收缩 → 总宽 2124 > 1920、会话区被推出视口。修复=WsDivider 常驻轮询把 OLAP 目标宽钳到 `min(saved, frW−sidebar−360)`（展开自动压缩、收起回偏好、`divDragging` 时跳过）。**经验：固定像素列宽 + 侧栏展收 = 必然溢出点，目标宽必须随可用视口 clamp，且 clamp 与用户拖拽互相不可打扰。** ②**左栏默认 150→210px**：库表/收藏/工作区/知识库 4 个横排标签需约 196px（gap14×3+padding24）远超 150px，末标签被挤出；tabs2 gap 14→10、padding 12→10。**经验：flex 横排多标签容器，默认宽按「标签总宽+间距」算，别拍脑袋。**
- 2026-08-31 **工作区按会话隔离（per-session）+ 面板跟随会话**：①工作区原为全局一份（新会话被灌旧标签/SQL）→ 改 per-session：host `ws.workspace.*` 支持 `sessionId`（落到 `workspace/<sessionId>/{sql,params,notes}/`，不传回退全局兼容）；client 缓存改 `wsMetaBySid`，持久化/恢复/工作区树全带 sessionId；**新会话目录无文件 → 保持初始 1 空 Tab1（纯新）**。②DSH details slot 切会话不更新 props.sessionId（实测对话区已切、面板仍绑旧 store）→ OlapPanel 轮询 sessions `current`（800ms）切 store。③DSH「新会话」按钮只创建不切 current → `wsFollowNewSessions`（1s 轮询 ids）新增 id 自动 open。**经验：DSH details/侧栏 slot 是「挂载时绑定 sessionId」的，切会话要自己跟随；`let` 变量声明必须在赋值之前（TDZ），跨函数提升声明但赋值顺序决定生死——踩过一回插件整体加载失败。**
